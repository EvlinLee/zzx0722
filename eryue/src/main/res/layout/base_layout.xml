<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <LinearLayout
        android:id="@+id/layout_fragment_navigation"
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_height="50dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/iv_fragment_back"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:padding="8dp"
            android:scaleType="fitXY"
            android:src="@drawable/icon_back" />

        <TextView android:id="@+id/tv_fragment_title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginRight="35dp"
            android:gravity="center"
            android:textSize="18dp"
            android:textColor="@color/black"/>

    </LinearLayout>

    <View android:id="@+id/view_fragment_navigationline"
        android:layout_width="match_parent"
        android:layout_height="1px"
        android:layout_below="@id/layout_fragment_navigation"
        android:background="@color/gray" />


    <FrameLayout
        android:id="@+id/fragment_main_body"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/view_fragment_navigationline"/>
</RelativeLayout>